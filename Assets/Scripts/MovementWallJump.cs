using UnityEngine;

/*
    onJumpInput
        if movementwallcling.trigger()
            modVelocity.TriggerY(wallJumpValue)
*/

public class MovementWallJump : MonoBehaviour
{
    public bool Trigger(MovementWallCling wallCling)
    {
        var result = false;

        //if (wallCling.Trigger())
        // todo: u know

        return result;
    }
}
