using UnityEngine;
using UnityEngine.InputSystem;

public class CoreInput : MonoBehaviour
{
    public PlayerInputManager manager;

    

    //public void Update()
    //{
    //    var m = playerInput.actions["Move"].ReadValue<float>();
    //    if (Mathf.Abs(m) > 0)
    //    {
    //        Debug.Log($"move value: {m}");
    //    }
    //}
}
